# Frontend environment variables
# Copy this to .env and fill in the values

# Must match API_KEY in the backend .env
VITE_API_KEY=test

# Base URL of the Hono backend (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000
